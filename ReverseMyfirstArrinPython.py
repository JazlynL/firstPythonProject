def reverse(lst):
    # A list is a data structure in Python that is a mutable,
    # or changeable, ordered sequence of elements. Each element
    # or value that is inside of a list is called an item.
    # Just as strings are defined as characters between quotes,
    # lists are defined by having values between square brackets [ ]
    array = list(reversed(lst))
    return array
